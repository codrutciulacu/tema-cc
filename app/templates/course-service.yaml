apiVersion: v1
kind: Service
metadata:
  name: course-app
  labels:
    {{- include "app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 3000
      name: course-http
      targetPort: course
      protocol: TCP
  selector:
     app.kubernetes.io/name: course
